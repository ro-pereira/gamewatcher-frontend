import { TcardChampionchipProps } from "@/src/Theme/type"
import { Box } from "@mui/material"

export const CardChampionchip = ({ upcomingGamesList }: TcardChampionchipProps) => {
    return (
        <Box></Box>
    )
}